version: 0.0
os: linux
files:
  - source: /index.html
    destination: /var/www/html/
hooks:
  BeforeInstall:
    - location: install_dependencies.sh.sh
      timeout: 300
      runas: root
    - location: start_server.sh.sh
      timeout: 300
      runas: root
 
